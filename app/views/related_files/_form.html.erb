<%= form_with(model: related_file) do |form| %>
  <% if related_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(related_file.errors.count, "error") %> prohibited this related_file from being saved:</h2>

      <ul>
        <% related_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :task_id %>
    <%= form.text_field :task_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
